import React, {Component} from 'react';
import Create from 'components/create';

class Boards extends Component {
  render() {
    return(
      <div>
        <Create subtitle="Create Board"/>
      </div>
    );
  }
}

export default Boards;
